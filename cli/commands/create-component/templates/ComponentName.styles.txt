import {
	ComponentNameStyleProps,
	ComponentNameStyles,
} from "./ComponentName.types";

export const getStyles = (
	props: ComponentNameStyleProps
): ComponentNameStyles => {
	const {className} = props;

	return {
		root: [className],
	};
};
