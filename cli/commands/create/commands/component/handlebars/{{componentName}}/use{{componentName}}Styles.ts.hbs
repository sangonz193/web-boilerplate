import { css, makeStyles } from "@fluentui/react"

import { getUuid } from "{{getUuidImportPathFromStyles}}"

export type {{componentName}}StyleProps = {
  className: string | undefined
}

export const {{camelCase componentName}}ClassNames = {
	wrapper: `wrapper-${getUuid()}`,
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const useStyles = makeStyles((theme) => {
  return {
    wrapper: {
    },
  }
})

export function use{{componentName}}Styles(props: {{componentName}}StyleProps) {
  const styles = useStyles()
  
  return {
    wrapper: css({{camelCase componentName}}ClassNames.wrapper, styles.wrapper, props.className),
  }
}
