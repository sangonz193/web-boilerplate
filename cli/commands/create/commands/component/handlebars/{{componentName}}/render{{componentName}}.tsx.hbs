import { Slots } from "{{slotsImportPath}}"
import { {{componentName}}Slots, {{componentName}}State } from "./{{componentName}}.types"

export function render{{componentName}}(state: {{componentName}}State, slots: Slots<{{componentName}}Slots>) {
	return (
		<slots.wrapper>
			{state.children}
		</slots.wrapper>
	)
}
