import React from "react"

import { SlotsProp } from "{{slotsPropImportPath}}"

export type {{componentName}}Slots = {
	wrapper: React.HTMLAttributes<HTMLDivElement>
};

export type {{componentName}}Props = {
	children?: React.ReactNode
	slots?: SlotsProp<{{componentName}}Slots>
}

export type {{componentName}}State = {
	slotProp: SlotsProp<{{componentName}}Slots>
	children: React.ReactNode
}

export type {{componentName}}Styles = {
	wrapper: string
}
